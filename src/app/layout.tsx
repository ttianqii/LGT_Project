// This file is intentionally left minimal. All layout and page logic is handled in [locale]/layout.tsx and [locale]/page.tsx for next-intl.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
